/* Empty stub: dosstage.c pulls in handlers.h (which includes <winsock2.h>
 * for the SOCKET type in handler prototypes) but uses no socket API itself.
 * The types it needs come from stubs/dosstage_env.h. */
#ifndef STUB_WINSOCK2_H
#define STUB_WINSOCK2_H
#endif
